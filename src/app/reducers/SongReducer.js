export default function reducer(state={
    songs: [],
    fetching: false,
    fetched: false,
    error: null,
}, action) {

    switch(action.type) {
        case 'UPDATE_SONGS': {
            return state;
        }

        case 'UPDATE_SONGS': {
            return state;
        }

        default: {
            return state;
        }
    }
}